# Diabetes Risk Prediction Web App - Python dependencies
#
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)
#   source .venv/bin/activate     (macOS / Linux)
#   pip install -r requirements.txt
#
# Versions are the ones this project was built and verified against.
# Loosen them only if you re-run `python -m ml.train` and the tests afterwards.

# --- Web framework -----------------------------------------------------------
Flask==3.1.3

# --- Machine learning pipeline -----------------------------------------------
scikit-learn==1.9.0
pandas==3.0.5
numpy==2.5.2
joblib==1.5.3

# --- Configuration -----------------------------------------------------------
python-dotenv==1.2.3

# --- LLM narrative layer -----------------------------------------------------
# No package needed. Groq, OpenAI and Gemini are all reached over plain HTTPS
# with the standard library, and the app falls back to its own knowledge-base
# writer when no provider is configured.
